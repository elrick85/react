/**
 * Created by zauri_000 on 13.10.2016.
 */

import * as React from 'react';
import * as ReactDOM from 'react-dom';
import {App} from './app/app';

ReactDOM.render(React.createElement(App), document.getElementById('body-container'));